import { Color } from "@/constants/Colors";
import { StyleSheet } from "react-native";

export const rnInputStyles = StyleSheet.create({
  inputStyles: {
    height: 54,
    borderWidth: 1,
    borderColor: Color.grey[200],
    borderRadius: 14,
    paddingHorizontal: 16,
  },
});
